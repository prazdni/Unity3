using System;
using System.Collections;
using System.Collections.Generic;
using MyLabyrinth;
using UnityEngine;
using UnityEngine.Events;


public sealed class Test : MonoBehaviour
{
    private void Start()
    {
        //XMLData data = new XMLData();
        //data.Save(new SavedData(){Name =  "Ball", Position = new Vector3Serializable(1, 2, 3), IsEnabled = true}, "XMLDATA.xml");
    }
}